import { View, Text } from "react-native";
import React from "react";
import { SearchBar } from "../../components";

const Map = () => {
  return (
    <View
      style={{
        marginTop: 60,
      }}
    >
      <SearchBar />
    </View>
  );
};

export default Map;
